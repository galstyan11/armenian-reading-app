services:

  app:
    build: ./backend
    container_name: app
    env_file:
      - ./backend/.env
    environment:
      STREAMLIT_LOG_LEVEL: debug

    ports:
      - "8501:8501"
    networks:
      - reading-app-net
    depends_on:
      db:
        condition: service_healthy

  db:
    image: mysql:8.0.43
    container_name: db
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: galstyanm2311
      MYSQL_DATABASE: reading_app
      MYSQL_ROOT_HOST: "%"
    command: --default-authentication-plugin=mysql_native_password
    networks:
      - reading-app-net
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s


networks:
  reading-app-net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.55.0.0/24
